# GHA_ReleaseUpdating-and-Syncing
Coming up with a Github Action tool, so that once a release branch is merged to master. The tool will activate and update all other active release branches and update them automatically with master.
